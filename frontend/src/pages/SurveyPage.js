import {useState} from "react"; import {useNavigate} from "react-router-dom"; import axios from "axios";
const interests={"자연 / 트레킹":"nature","맛집 탐방":"food","카페 투어":"cafe","문화 / 박물관":"culture","쇼핑":"shopping","해수욕":"beach"}; const locations={"제주시":[33.4996,126.5312],"서귀포시":[33.253,126.56],"애월":[33.463,126.31],"성산":[33.458,126.942]}; const API=process.env.REACT_APP_API_BASE_URL||"http://localhost:8000";
export default function SurveyPage(){const nav=useNavigate(),[selected,setSelected]=useState([]),[loc,setLoc]=useState("제주시"),[form,setForm]=useState({travel_date:new Date().toISOString().slice(0,10),start_time:"09:00",end_time:"19:00",weather_tolerance:"any"}),[error,setError]=useState("");const toggle=x=>setSelected(s=>s.includes(x)?s.filter(v=>v!==x):[...s,x]);const submit=async()=>{if(!selected.length)return setError("관심 활동을 선택해주세요.");const [start_lat,start_lng]=locations[loc];try{const data=(await axios.post(`${API}/recommend`,{...form,style:"nature",place:form.weather_tolerance==="indoor"?"indoor":"outdoor",activity:"active",interests:selected.map(x=>interests[x]),start_lat,start_lng,top_n:5})).data;sessionStorage.setItem("plans",JSON.stringify(data));nav("/plans",{state:{data}})}catch{setError("추천 서버 연결에 실패했습니다.")}};return <main className="page"><h1>나만의 제주 여행 계획</h1><p>날짜, 취향, 숙소 위치를 입력하면 세 가지 코스를 추천합니다.</p><label>여행 날짜 <input type="date" value={form.travel_date} onChange={e=>setForm({...form,travel_date:e.target.value})}/></label><label>시작 <input type="time" value={form.start_time} onChange={e=>setForm({...form,start_time:e.target.value})}/></label><label>종료 <input type="time" value={form.end_time} onChange={e=>setForm({...form,end_time:e.target.value})}/></label><h2>관심 활동</h2>{Object.keys(interests).map(x=><button className={selected.includes(x)?"selected":""} onClick={()=>toggle(x)} key={x}>{x}</button>)}<h2>숙소 위치</h2>{Object.keys(locations).map(x=><button className={loc===x?"selected":""} onClick={()=>setLoc(x)} key={x}>{x}</button>)}<h2>날씨</h2><button onClick={()=>setForm({...form,weather_tolerance:"any"})}>날씨 무관</button><button onClick={()=>setForm({...form,weather_tolerance:"indoor"})}>실내 위주</button>{error&&<p className="error">{error}</p>}<button className="primary" onClick={submit}>A/B/C 여행안 만들기</button></main>}
